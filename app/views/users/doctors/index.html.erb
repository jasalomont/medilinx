<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title></title>

</head>
<body>
  <div class="page-header">
    <h3>Doctores</h3>
  </div>
</body>

<%= search_form_for @q do |f| %>
  <p class="lead">Narrow results:</p>

  <div class="form-group">
    <%= f.label :title_cont, "Title containing" %>
    <%= f.text_field :title_cont, :class => "form-control", :placeholder => "Enter a few characters" %>
  </div>

  <div class="form-group">
    <%= f.label :year_gteq, "Released after" %>
    <%= f.text_field :year_gteq, :class => "form-control", :placeholder => "Year greater than or equal to" %>
  </div>

  <div class="form-group">
    <%= f.label :year_lteq, "Released before" %>
    <%= f.text_field :year_lteq, :class => "form-control", :placeholder => "Year less than or equal to" %>
  </div>

  <%= f.submit :class => "btn btn-primary btn-block" %>

  <a href="/movies" class="btn btn-default btn-block">Clear filters</a>
<% end %>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Nombre</th>
        <th>Especialidad</th>
        <th>Ubicación</th>
        <th></th>
      </tr>


<% @doctors.each do |doctor| %>
<tr>
  <td><a href="/doctors/<%= doctor.id %>"> <%= doctor.practice_name  %></a></td>
  <td><%=  %></td>
  <td><%= doctor.city_work %></td>
  <td><a href="/doctors/<%= doctor.id %>"class="btn btn-primary"> <i class="fa fa-search-plus" aria-hidden="true"></i></a>
    <% if current_doctor.email == "admindoc@medilinx.net" %>
      <a href="/delete_doctor/<%= doctor.id %>" class="btn btn-danger" rel="nofollow">Delete</a>
    <% end %>
  </td>


</tr>
<% end %>


</html>
