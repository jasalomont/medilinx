<div class="page-header">
  <h1>
    Pacientes conectados
    <!-- <a href="/broker_insurers/new" class="btn btn-lg btn-success">New Broker insurer</a> -->
  </h1>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Paciente</th>
        <th>Aseguradora</th>
        <th>Póliza</th>
        <th>Notas</th>
        <th></th>
      </tr>

      <% @broker_relations.each do |broker_relation| %>
      <tr>
        <td><%= broker_relation.patient.first_name + " " + broker_relation.patient.last_father + " " + broker_relation.patient.last_mother %></td>
        <td><%= broker_relation.insurance_company %></td>
        <td><%= broker_relation.contract_code %></td>
        <td><%= broker_relation.broker_notes %></td>

        <td>
          <a href="/broker_relations/<%= broker_relation.id %>" class="btn btn-sm btn-primary">Ver</a>
          <!-- <a href="/broker_relations/<%= broker_relation.id %>/edit" class="btn btn-warning">Edit</a>
          <a href="/delete_broker_relations/<%= broker_relation.id %>" class="btn btn-danger" rel="nofollow">Delete</a> -->
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
